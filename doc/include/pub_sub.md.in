Publishers and Subscribers communicate over topics identified by string names as
in ROS. A simple publisher is shown in the following example program:

```cpp
@EXAMPLE_PUBLISHER_CPP@
```

A subscriber can be listen and respond to messages sent by a subscriber. The
following is a sample program that demonstrates this:

```cpp
@EXAMPLE_SUBSCRIBER_CPP@
```
